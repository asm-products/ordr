// Global page header

%header.main_header
  %a.logo{:href => "#"} [LOGO]
  %div
    - if current_user
      = link_to "Sign out", url_for(controller: 'sessions', action: 'destroy')
    - else
      = link_to "Sign in", url_for(controller: 'sessions', action: 'new')
