<%= simple_form_for(@apply) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :sent %>
    <%= f.input :notes %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
