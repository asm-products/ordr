%p#notice= notice
%p
  %strong Sent:
  = @apply.sent
%p
  %strong Notes:
  = @apply.notes
= link_to 'Edit', edit_job_apply_path(job_id: @job.id, id: @job.apply.id)
|
\#{link_to 'Back', :back}

= link_to "new contact", new_job_apply_contact_path(@job, @apply)


%table
  %tbody
    - @apply.contacts.each do |comment|
      %tr
        - if !comment.new_record?
          %td
            Name: #{contact.name}
          %td
            Company:  #{contact.company}
          %td
            %td= link_to 'Edit', edit_job_apply_contact_path(@apply, contact)
            %td= link_to 'Delete', job_apply_contact_path(@apply, contact), method: :delete, data: { confirm: 'Are you sure?' }
