<%= simple_form_for(@network) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :contact %>
    <%= f.input :notes %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
